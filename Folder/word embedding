text의 전처리 과정


tokenizer ==> 공백
stemmer ==> 데이터의 특징에서 불필요한 것을 제거하는 어간 추출(문맥 X==> 성능 떨어짐)
lemmatizer ==> 표제어 추출( 사전적인 의미)
stopwords==> 불용어(불필요한 용어)
Integer Encoding ==> 단어 수치화

## word embedding
1. sparse representation (희소 표현)
 - 벡터, 행렬의 값이 대부분 0으로 표현
 - ex one-hot-vector
2.  dense representation (밀집 벡터)
 - 사용자가 설정한 값으로 모든 단어의 벡터 차원을 맞춤
 - 실수로 표현


ipynb lang 설명
각각의 문자를 Unicode 숫자로 변환 
- code_value=ord(i) #문자를 Unicode 정수로 반환
tokenizer1 ipynb 설명
 - 토큰화: tokenzier
 - 품사태깅: pos_tag(tokenized)
 - okt 형태소 분석
 - morphs: 형태소 추출
 - nonus: 명사 추출
 - pos : 품사 부착
 - phrases:어절
